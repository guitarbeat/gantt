# Academic Preset - Default detailed view
# This is the baseline configuration for academic planning

# Inherit all settings from base.yaml
# This preset represents the current detailed academic view
# No overrides needed - this is the default configuration
